# Check if we have homogeneous types
def homogeneous_types(data):
    # iseq = iter(seq)
    # first_type = type(next(iseq))
    # return first_type if all( (type(x) is first_type) for x in iseq ) else False
    return data
